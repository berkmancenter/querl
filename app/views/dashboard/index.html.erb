<div class="col-lg-12">
  <h1>Dashboard</h1>
</div>

<div class="col-lg-6">
  <h2>Your Projects</h2>
  <% unless @your_projects.nil? %>
    <%= render :partial => 'shared/project', :locals => {:projects => @your_projects} %>	
  <% else %>
    <h3>No projects yet.</h3>
  <% end %>
  <p><%= link_to "Add New Project", new_project_path %></p>
</div>

<div class="col-lg-6">
  <h2>All Projects</h2>
  <% unless @all_projects.nil? %>
    <%= render :partial => 'shared/project', :locals => {:projects => @all_projects} %>	
  <% else %>
    <h3>No projects yet.</h3>
  <% end %>
</div>